services:
  lab:
    build: .
    environment:
      HF_HOME: /cache/huggingface
      MPLBACKEND: Agg
    volumes:
      - model-cache:/cache/huggingface
      - ./output:/app/output
      - ./data:/app/data

volumes:
  model-cache:
