# Copy to .env only if you want the real-model path. The lab runs without it.
LLM_MODE=scripted

# Needed only when LLM_MODE=gemini. Get one from https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash

JWT_SECRET=dev-only-secret-change-me
LOG_LEVEL=info
