{{- range $name, $port := dict "chat-backend" 3001 "chat-postgres" 5432 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  namespace: executor-app
spec:
  type: ClusterIP
  selector: {app: {{ $name }}}
  ports: [{port: {{ $port }}, targetPort: {{ $port }}}]
{{- end }}
